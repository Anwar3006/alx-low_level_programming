#ifndef FUNCTION_LIKE_MARCOS_H
#define FUNCTION_LIKE_MARCOS_H

/**
 * Header file defines a function-like marcos ABS(x)
 * that computes the absolute value of a number
 */

#define ABS(x) ((x) < 0 ? (-x) : (x))

#endif
