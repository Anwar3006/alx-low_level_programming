#include "main.h"

/**
 * _strlen -  returns the length of a string
 * @s: string to count
 *
 * Return: void
 */

int _strlen(char *s)
{
	int d;

	for (d = 0; s[d] != '\0'; d++)

	
		return (0);

}

